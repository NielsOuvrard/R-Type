name: Push to Destination Repository
on:
  workflow_dispatch:
  push:
    paths:
      - 'docs/*'
jobs:
  push:
    runs-on: ubuntu-latest

    steps:
      - name: Check out source repository
        uses: actions/checkout@v2

      - name: Push to Destination Repository
        env:
          DESTINATION_REPO_TOKEN: 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
        run: |
          git config --global user.email "clovis.rabot@epitech.eu"
          git config --global user.name "rclovis"

          git clone https://github.com/rclovis/R-Type-Documentation.git

          rm -r R-Type-Documentation/r-type/docs
          cp -r docs R-Type-Documentation/r-type

          cd R-Type-Documentation

          git add .
          git commit -m "Update from source repository"
          git push https://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@github.com/rclovis/R-Type-Documentation.git HEAD:main